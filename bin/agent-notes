#!/usr/bin/env bash
# agent-notes — thin wrapper that delegates to the Python CLI.
set -e
AGENT_NOTES_DIR="$(cd "$(dirname "$0")/.." && pwd)"
export AGENT_NOTES_DIR
export PYTHONPATH="$AGENT_NOTES_DIR/lib${PYTHONPATH:+:$PYTHONPATH}"
exec python3 -m agent_notes "$@"
